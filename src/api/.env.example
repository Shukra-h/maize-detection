CORS_ORIGINS=https://your-frontend.vercel.app,http://localhost:5173,http://localhost:3000
# Optional override if you store the model in another path
# MODEL_PATH=/app/best_model.keras
# Optional override if you want to load class names from a JSON artifact file
# CLASS_NAMES_PATH=/app/class_names.json
# Optional override if you want to supply the training class order explicitly
# CLASS_NAMES=Corn_(maize)___Cercospora_leaf_spot Gray_leaf_spot,Corn_(maize)___Common_rust_,Corn_(maize)___Northern_Leaf_Blight,Corn_(maize)___healthy,unknown_unclassified
# Optional threshold for the middle "likely" decision band
# LIKELY_CONFIDENCE_THRESHOLD=0.50
